name: ${project.name}
main: com.poixson.weblinkmc.WebLinkPlugin
version: ${project.version}
api-version: ${spigot-short-version}
depend:
  - pxnPluginLib
softdepend:
  - Vault
authors: [ lorenzop ]
website: ${project.url}
description: ${project.description}
